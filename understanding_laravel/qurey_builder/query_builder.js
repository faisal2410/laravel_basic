/*
Laravel's query builder is a set of classes and methods that provide
a simple and elegant way to interact with databases


The first() method returns an object, which can be accessed using
the arrow notation

The value() method is used to retrieve a single value from a result set.

The find() method is used to retrieve a single record by its primary key

The pluck() method is used to retrieve a single columns value from the first
result of a query.

insertOrIgnore() method is a method that allows you to insert data into a 
database table only if the data doesn't already exist in the table

The upsert() method on the query builder in laravel is a powerful tool for
performing insert or update operations on large datasets

The insertGetId() method allows you to insert a new record into a table
and retrieve its id in a single query.


*/ 